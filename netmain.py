import util


def test():
    pass
    # create instance of the server
    # server = Server(49000)
    # server.start()

    # start the server on a different thread
    # t = Thread(target=server.start, args=())
    # t.start()

    # create client instance
    # client1 = Client("Client1", 49000)

    # send files to server, with default path of "." (current directory)
    # client1.update(server.ip, server.tcp_port)


if __name__ == '__main__':
    # upackets = util.read_file(file_path="snippets.txt", sport=8080, dport=8000)
    pass
